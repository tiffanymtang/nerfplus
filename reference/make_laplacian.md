# Construct a dense graph Laplacian with optional ridge adjustment

Construct a dense graph Laplacian with optional ridge adjustment

## Usage

``` r
make_laplacian(A, lambda_l = 0, nodedegrees = NULL)
```
