# File input module - UI

File input module - UI

File input module - Server

## Usage

``` r
fileInputUI(
  id,
  label = "File Upload",
  accept = c(".csv", ".txt", ".rds"),
  show_accept = TRUE,
  tooltip = NULL
)

fileInputServer(id, default_data = NULL)
```
