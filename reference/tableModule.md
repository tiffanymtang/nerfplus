# Basic table module - Server

Basic table module - Server

## Usage

``` r
tableServer(
  id,
  table_fun,
  table_options = TRUE,
  mode = c("DT", "kable"),
  caption = NULL,
  border = FALSE,
  spinner = TRUE,
  error_msg = "",
  ...
)
```
