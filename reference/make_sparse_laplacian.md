# Construct a sparse graph Laplacian with optional ridge adjustment

Construct a sparse graph Laplacian with optional ridge adjustment

## Usage

``` r
make_sparse_laplacian(A, lambda_l = 0, nodedegrees = NULL)
```
